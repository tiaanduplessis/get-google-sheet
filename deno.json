{
  "lint": {
    "files": {
      "exclude": ["node_modules", "dist", "package-lock.json"]
    },
    "rules": {
      "tags": ["recommended"],
      "exclude": ["no-explicit-any", "ban-ts-comment"]
    }
  },
  "fmt": {
    "files": {
      "exclude": ["node_modules", "dist", "package-lock.json"]
    }
  }
}
